flask==3.0.2
flask-cors==4.0.0
pymongo==4.6.2
python-dotenv==1.0.1
pyjwt==2.8.0
bcrypt==4.1.2
pandas==2.2.0
numpy==1.26.4
scikit-learn==1.4.1.post1
requests==2.31.0
gunicorn==21.2.0
mongomock==4.1.2
# Email delivery — Resend HTTP API (works on Render free tier; no SMTP ports needed)
# email_service.py uses the `requests` library (already above) to call api.resend.com over HTTPS.
# No additional package install required. Set RESEND_API_KEY + RESEND_FROM_ADDRESS in Render.
