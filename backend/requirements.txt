flask==3.0.2
flask-cors==4.0.0
pymongo==4.6.2
python-dotenv==1.0.1
pyjwt==2.8.0
bcrypt==4.1.2
pandas==2.2.0
numpy==1.26.4
scikit-learn==1.4.1.post1
requests==2.31.0
gunicorn==21.2.0
mongomock==4.1.2
joblib==1.4.2
xgboost==2.0.3
# ── Optional: Real DistilBERT NLP (removes keyword-only fallback, ~800 MB) ──
# Uncomment the two lines below and re-run pip install -r backend/requirements.txt
# transformers==4.40.0
# torch==2.2.2
# Email delivery uses Python's built-in smtplib (no extra package needed).
# Set SMTP_EMAIL and SMTP_PASSWORD in your environment (Gmail App Password).
